name: Backend CI

on:
  workflow_call:
  pull_request:
    branches: [main]
    paths:
      - "src/Backend/**"
      - "src/Shared/**"
      - "tests/LS.Tests.Unit/**"
      - "tests/LS.Tests.Integration/**"
      - ".github/workflows/backend-ci.yml"
  push:
    branches: [main]
    paths:
      - "src/Backend/**"
      - "src/Shared/**"
      - "tests/LS.Tests.Unit/**"
      - "tests/LS.Tests.Integration/**"
      - ".github/workflows/backend-ci.yml"

permissions:
  contents: read

jobs:
  backend:
    name: Backend build and tests
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore API
        run: dotnet restore src/Backend/Api/LS.Api/LS.Api.csproj

      - name: Restore unit tests
        run: dotnet restore tests/LS.Tests.Unit/LS.Tests.Unit.csproj

      - name: Restore integration tests
        run: dotnet restore tests/LS.Tests.Integration/LS.Tests.Integration.csproj

      - name: Build API
        run: dotnet build src/Backend/Api/LS.Api/LS.Api.csproj --configuration Release --no-restore

      - name: Run unit tests
        run: dotnet test tests/LS.Tests.Unit/LS.Tests.Unit.csproj --configuration Release --no-restore --logger trx

      - name: Run integration tests
        run: dotnet test tests/LS.Tests.Integration/LS.Tests.Integration.csproj --configuration Release --no-restore --logger trx
